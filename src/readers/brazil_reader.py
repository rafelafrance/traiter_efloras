"""Parse Brazil Flora html pages."""

# from ..pylib.util import BRAZIL_DIR


def brazil_reader(args, _):
    """Parse the downloaded webpages."""
    rows = []
    for family in args.family:
        print(family)

    return rows
